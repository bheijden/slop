{
  "$comment": "Config for this repo. Copy it next to your docs and edit. No select list: every shipped set runs, and each rule's own default decides whether it is part of that.",
  "ignore": [
    "rather-than"
  ],
  "exclude": [
    "vendor/",
    "node_modules/"
  ],
  "$comment2": "These docs are table-heavy, and parallel table rows trip echo-triad.",
  "skipTables": true,
  "maxPer1000": 3.0,
  "$comment4": "rather-than discriminates well across mixed registers (2 human against 24 AI hits in the audit) but is ordinary English in engineering documentation, which is what this repo is. A register judgement, not a rule defect, so it is ignored here rather than turned off for everyone."
}
